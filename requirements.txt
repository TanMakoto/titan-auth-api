# ============================================================
# Cloud requirements for Face AI API (Render.com)
# Python 3.11.9 required — see runtime.txt
# ============================================================

# OpenCV headless — avoids "libGL" missing error on Linux
opencv-python-headless>=4.8.0

numpy
fastapi
uvicorn[standard]
python-multipart
gdown

# TensorFlow CPU-only for Python 3.11 (no GPU needed)
tensorflow-cpu>=2.15.0
tf-keras

# DeepFace — pinned to tested version
deepface==0.0.99
